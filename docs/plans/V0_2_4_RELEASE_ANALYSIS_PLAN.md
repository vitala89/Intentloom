# v0.2.4 Release Analysis Plan

The first increment correlates one local Git release timeline with one explicit
GitHub or GitLab export. It reports verified, missing, conflicting, ambiguous,
and unsupported evidence only. It does not make compliance, conformance, or
causality claims.

The operation is pure, deterministic, project-key isolated, and bounded by the
already normalized inputs. CLI and MCP exposure follow after dogfooding.
