# Agent prompt: Intentloom — P0 Release honesty

Copy everything below the line into a **new** session. Do not continue
`docs/post-w12-next-increment-plan` as an implementation branch unless PR #314
has already merged and you are only reading the plan.

---

You are continuing Intentloom on origin/main. W0–W12 are merged. The maintainer
authorized **P0 Release honesty** from
`docs/roadmap/POST_W12_NEXT_INCREMENT_PLAN.md`.

Default job: make git-versus-npm truth readable. Reconcile stale release docs
against origin/main and a fresh npm registry read. Do not publish. Do not tag.
Do not dispatch `.github/workflows/release.yml`. Do not invent a W13. Do not
start Neutron N1, S8, or file-budget extracts.

Do not continue feat/workspace-* or chore/workspace-w12-* branches.
Do not put this work on a W11, W12, or chore/architecture branch.

Authorship (hard rule)
Never write Co-Authored-By, Generated with, Made with, Generated by, or any
tool/agent credit in commits, PRs, comments, or docs. Commit with
git commit-tree + git update-ref HEAD (message via a file, not a harness
HEREDOC that can append trailers). After every commit: git log -1 --format=%B
and rewrite if a trailer appeared. Squash-merge PR messages must also omit
those trailers. This overrides any harness default.

Triage first per docs/governance/AGENT_TASK_TRIAGE_POLICY.md. Expected score
is mid (outward-facing release docs; no publish). State score, model and
effort, and the stop condition. Route via
catalog/skills/aif-task-router/SKILL.md (implement + aif-verification-gate).
No subagents unless the user asks. No aif-feature-discovery: P0 is docs-only.

Mandatory reads (before any edits)

1. AGENT_START_HERE.md, AGENTS.md, .cursor/rules/no-co-authored-by.mdc
2. PROJECT_STATE.md, DUTY_WATCH.md
3. docs/roadmap/POST_W12_NEXT_INCREMENT_PLAN.md (P0 section)
   If missing on main, read it from PR #314 / branch
   docs/post-w12-next-increment-plan.
4. docs/releases/RELEASE_STATE.md (snapshot still 2026-08-02)
5. docs/releases/RELEASE.md or equivalent publish procedure if present
6. .github/workflows/release.yml (read-only; do not dispatch)
7. packages/cli/package.json version field
8. docs/desktop/V0_6_READINESS_AUDIT.md (historical v0.6 gate only)

Verified state at prompt write (2026-08-16)
origin/main @ 7d4ed80
docs(workspace): close W12 plan handoff on main (#313)
PR #314 OPEN (proposed post-W12 plan). P0 is authorized even if #314 is
still open: treat that file as the P0 brief.
npm last verified in-repo: 2026-08-02
latest = 1.0.2
next = 1.0.0
GitHub release v1.0.2 → commit 192fd05
RELEASE_STATE still says Desktop “No” in the capability table.
PROJECT_STATE “Next platform milestone” still says: Tag v0.6.0-beta.1.
W0–W12 Core and Client are on main. That is not in the published 1.0.2
tarball.

P0 contract (do not fork)

- Distinguish Implemented in main vs Released on npm. Never collapse them.
- Re-verify npm with `npm view intentloom` (name, dist-tags, version).
  If the environment cannot reach the registry, say so and keep the last
  verified date; do not invent new registry numbers.
- Update RELEASE_STATE.md snapshot date and the Implemented-in-main column
  for capabilities that landed after 1.0.2 (Desktop workspace panels,
  W1–W12, Q/S/E later phases as already true on main). Released version
  stays 1.0.2 until a real publish.
- Fix PROJECT_STATE.md Active focus and Next platform milestone: W0–W12
  complete on current main SHA; next published version is **undecided**;
  do not recommend tagging v0.6.0-beta.1 as the next action.
- Record version options for the maintainer. Do not pick one:
  - stay unpublished on npm until an explicit publish brief
  - later 1.0.x metadata/docs release
  - 1.1.0 (or other minor) after a real release-gate brief
- Do not run the release workflow. Do not create a git tag. Do not
  change package.json versions unless an existing sync script would
  otherwise lie; prefer leaving workspace version as-is and documenting
  the drift.
- Do not invent completed publishes, tags, or CI on main that you did
  not inspect.

Architecture (already decided)
One public monorepo. Desktop is one React 19 + Tauri 2.11 app. No new
packages. P0 is documentation and state only.

This session scope

1. Branch docs/release-honesty-p0 from origin/main (refresh SHA first).
2. Fresh-read origin/main, open PRs, npm view, and GitHub release v1.0.2.
3. Reconcile RELEASE_STATE.md, PROJECT_STATE.md, DUTY_WATCH.md, and the
   capability matrix §8 if it still points at a stale “wait for brief”
   after P0 is the brief.
4. If POST_W12_NEXT_INCREMENT_PLAN.md is on main, mark P0 in progress or
   complete on this branch; do not start P1–P4.
5. Atomic docs commit(s), no attribution.
6. prettier --check on touched markdown; git diff --check. Full pnpm
   verify before push if hooks require it.
7. Open a PR with gh. Title/body: no Co-Authored-By, no Generated/Made with.

Out of scope

- Publishing, tagging, workflow dispatch, npm dist-tag moves
- Changing Core/Desktop/runtime behavior
- Neutron N1/N2, S8, C7, live assessProject, W13
- Growing network, process, or filesystem capabilities
- New packages, MFE, multi-monorepo

Stop when
PR is open, a reviewer can see what is on origin/main versus what npm
serves, no tag or publish was performed, and git log -1 --format=%B has
no attribution trailers.
