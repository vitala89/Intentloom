# Security policy

Treat repository inputs, templates, and generated content as untrusted until validated.

## Rules

Never execute template content, read secrets, or write outside the supplied project root. Require an explicit human choice for replacement.
